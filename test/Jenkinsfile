pipeline {
    agent any

    stages {
        stage('build') {
            steps {
                sh 'ls -l'
            }
        }

        stage('test') {
            steps {
                sh 'ls -a'
            }
        }

        stage('deploy') {
            steps {
                sh 'ls'
            }
        }
    }
}
